name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    strategy:
      matrix:
        include:
          - os: macos-latest
            rid: osx-arm64
          - os: macos-latest
            rid: osx-x64
          - os: ubuntu-latest
            rid: linux-x64
          - os: ubuntu-latest
            rid: linux-arm64

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout dafny2js
        uses: actions/checkout@v4

      - name: Checkout dafny
        run: git clone --depth 1 --branch master https://github.com/dafny-lang/dafny.git ../dafny

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Publish
        run: dotnet publish -c Release -r ${{ matrix.rid }} --self-contained /p:PublishSingleFile=true

      - name: Package
        run: |
          cd bin/Release/net8.0/${{ matrix.rid }}/publish
          tar czf ${{ github.workspace }}/dafny2js-${{ matrix.rid }}.tar.gz dafny2js

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: dafny2js-${{ matrix.rid }}
          path: dafny2js-${{ matrix.rid }}.tar.gz

  release:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          merge-multiple: true

      - name: Create release and upload assets
        uses: softprops/action-gh-release@v2
        with:
          files: dafny2js-*.tar.gz
